import { LockKeyhole, ShieldCheck, UserRound } from 'lucide-react';import { PageHeader } from '../components/UI';import { useAuth } from '../context/AuthContext';
export default function SettingsPage(){const {user}=useAuth();return <><PageHeader eyebrow="Account" title="Settings" description="Profile and security controls for your MEDISCOPE account."/><div className="settings-grid"><section className="panel setting-card"><UserRound/><div><h3>Profile</h3><p>{user?.first_name} {user?.last_name}<br/>{user?.email}</p></div></section><section className="panel setting-card"><ShieldCheck/><div><h3>Role & account</h3><p>{user?.role}<br/>{user?.account_status}</p></div></section><section className="panel setting-card"><LockKeyhole/><div><h3>Multi-factor authentication</h3><p>{user?.mfa_enabled?'Enabled':'Available through the secure authentication API.'}</p></div></section></div></>}
